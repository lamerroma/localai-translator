# Changelog

## v1.7 (2026-06-03)
- Нові налаштування перекладу (адмін-панель + `translator_config.json`):
  - `temperature` (0–2, за замовч. 0.7) — точність/творчість відповіді моделі
  - `retry` (1–6, за замовч. 2) — кількість повторів при невдалому батчі
  - `insert_mode` (replace/append/prepend) — режим вставки перекладу в DOCX
  - `separator` — роздільник між оригіналом і перекладом (append/prepend)
  - `custom_prompt` — кастомна інструкція, що додається перед стандартним промптом
- `docx_translate.py`: `translate_docx` та `_apply_translation` підтримують `insert_mode` + `separator`
- Admin UI: роздільник автоматично ховається в режимі replace

## v1.5 (2026-06-03)
- Новий модуль `docx_translate.py` — повний порт XML-логіки DocuTranslate
  - Рекурсивна обробка гіперпосилань, smartTag, sdtContent (раніше текст губився)
  - Текст у текстових блоках всередині фігур тепер перекладається
  - Правильна обробка меж полів fldChar (TOC, PAGE — не потрапляють у переклад)
  - Очищення TOC/PAGE полів щоб не зламати структуру документу
- `translate_server.py`: старі DOCX helper-функції замінені на `docx_translate.py`

## v1.4 (2026-06-03)
- Переклад DOCX: новий JSON-батч підхід (за зразком DocuTranslate)
  - Всі сегменти документу збираються разом і перекладаються пакетами
  - Кожен форматний сегмент (жирний, курсив тощо) отримує власний переклад
  - Значно менше запитів до Ollama (замість N абзаців → кілька батчів)
  - Зникла проблема пропорційного розподілу слів між сегментами

## v1.3 (2026-06-03) ✓ стабільна
- Адмін-сторінка: залишено тільки статистику і налаштування
- Кнопка "Очистити статистику" з підтвердженням
- Новий endpoint `POST /admin/stats/clear`
- Кнопка "← На головну" в адмін-панелі
- **Якість перекладу:** достатній рівень, поточна реалізація зафіксована

## v1.2 (2026-06-03) ✓ стабільна
- Індикатор підключення до Ollama (зелена/червона точка)
- Вибір моделі зі списку доступних на сервері (під вибором мов)
- Автооновлення статусу кожні 30 секунд
- Новий endpoint `GET /models`
- Адреса Ollama за замовчуванням змінена на `http://127.0.0.1:11434/v1`
- **Примітка:** різні моделі по-різному дотримуються форматування Markdown

## v1.1 (раніше)
- Адреса за замовчуванням: `http://192.168.88.59:11434/v1`
- Модель за замовчуванням: `rinex20/translategemma3:12b`
- Розмітка результату через marked.js (Markdown → HTML)

## v1.0 (раніше)
- Перший публічний реліз
- Переклад тексту та файлів (PDF, DOCX, TXT)
- Side-by-side layout (оригінал | переклад)
- SSE стрімінг токенів
- Базова авторизація для /admin
- Статистика перекладів (SQLite)
